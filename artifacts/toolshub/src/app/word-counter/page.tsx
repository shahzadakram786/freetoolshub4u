import { WordCounter } from "@/views/word-counter";

export default function Page() {
  return <WordCounter />;
}

export const metadata = {
  title: "Word & Character Counter",
  description: "Free online word counter, character counter, and reading time estimator.",
};
